
$for(var in expr) {
    text
    $cycle{text1}{text2}{text3}                     - по очереди
    $cycle(i,j){text}                               i,j например (1,5) на первом витке из 5ти
    $cycle("odd"){text}   $//  $cycle(1,2){text}
    $cycle("even"){text}  $//  $cycle(2,2){text}
    $cycle("first"){text}
    $cycle("not first"){text}
    $cycle("last"){text}
    $cycle("not last"){text}
} $separator {
    text
} $else {
    text
}

